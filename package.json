{
  "name": "gpustat",
  "version": "0.1.0",
  "description": "web app for all gpu's gpustat in server",
  "main": "server.js",
  "dependencies": {
    "express": "^4.16.4"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "lxc",
  "license": "ISC"
}
